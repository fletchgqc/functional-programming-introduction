package com.gft.fpintroduction.list;

import java.util.ArrayList;

/**
 * Task: complete the function forTail so that it works like forEach, except that it only applies the function to
 * the tail of the List (i.e. to every element except the first one).
 *
 * Once you are finished, the test in TailingArrayListTest will pass (delete the @Disabled annotation).
 *
 * Try it without looking at the method signature or the implementation of forEach.
 */
public class TailingArrayList<E> extends ArrayList<E> {

    /**
     * Note: method signature is wrong
     */
    public void forTail() {
    }

}
